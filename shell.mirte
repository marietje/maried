includes:
  - threadPool.mirte
definitions:
  shellServer:
    implementedBy: maried.shell.ShellServer
    settings:
      socketPath: {type: str, default: maried.socket}
      threadPool: {type: threadPool}
      manager: {type: manager}
    run: True
